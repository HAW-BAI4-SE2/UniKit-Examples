# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
POST    /anmelden 					controllers.Application.takepart()
POST    /ansehen 					controllers.Application.watchParticipants()
POST    /abmelden 					controllers.Application.signOut()
POST    /meineVeranstaltungen		controllers.Application.allMyCourses()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)




